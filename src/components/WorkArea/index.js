import React from 'react';

const WorkArea = () => (
  <div
    className="h-screen bg-gray-200 dark:bg-gray-800"
    style={{
      width: 'calc(100vw - 32rem)',
      height: 'calc(100vh - 4rem)',
    }}
  ></div>
);

export default WorkArea;
