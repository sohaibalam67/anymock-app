import React from "react";

const WorkArea = () => (
  <div
    className="h-screen bg-gray-900"
    style={{
      width: "calc((100vw - 32rem) - 2px)",
      height: "calc((100vh - 4rem) - 1px)",
    }}
  ></div>
);

export default WorkArea;
