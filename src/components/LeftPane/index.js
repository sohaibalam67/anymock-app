import React from "react";

const LeftPane = ({ params }) => (
  <div
    className="h-screen w-64 bg-charcoal-dark border-r border-charcoal-light"
    style={{ height: "calc((100vh - 4rem) - 1px)" }}
  ></div>
);

export default LeftPane;
